// components/Nav.tsx
// Re-export shim — implementation moved to components/nav/Nav.tsx as part
// of the components/ reorganization (nav/, ui/, profiles/, doctor/,
// blackbox/, dashboard/). Kept in case any external code still imports
// from the old flat path.
export { default } from "./nav/Nav";
