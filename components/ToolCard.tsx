// components/ToolCard.tsx
// Re-export shim — implementation moved to components/ui/ToolCard.tsx.
// Kept so existing imports (`@/components/ToolCard`) keep working.
export { default } from "./ui/ToolCard";
