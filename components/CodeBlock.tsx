// components/CodeBlock.tsx
// Re-export shim — implementation moved to components/ui/CodeBlock.tsx.
// Kept so existing imports (`@/components/CodeBlock`) keep working.
export { default } from "./ui/CodeBlock";
